<!doctype html>
<html lang="en">
    <head>
        <%- include('components/head'); %>
    </head>

    <body>
        <div class="container">
            <% for (let i = 0; i < data.length; i++) { %>
                <p><%= i + 1 %>. <%= data[i] %></p>
             <% } %>
        </div>
    </body>
</html>